# =============================================================================
# Ctrl-Alt-Play Panel Configuration
# =============================================================================
# 
# 🚀 QUICK START: Run `./quick-deploy.sh` to auto-generate this file with secure defaults
# 📚 For manual setup: Copy this file to .env and customize the values below
# 🔧 Advanced config: See docs/configuration.md for all available options
#

# =============================================================================
# 🎮 BASIC CONFIGURATION (Required)
# =============================================================================

# Environment mode - use 'production' for live deployments
NODE_ENV=development

# Main application port - the port your panel will run on
PORT=3000

# Panel branding and contact
PANEL_NAME="Ctrl-Alt-Play"
PANEL_URL=http://localhost:3000
ADMIN_EMAIL=admin@example.com

# =============================================================================
# 🔐 SECURITY SECRETS (Auto-generated recommended)
# =============================================================================
# 
# ⚠️  IMPORTANT: Generate secure random values for production!
# 💡 TIP: Run `./scripts/generate-secrets.sh` to auto-generate these
#

# JWT signing secret - used for user authentication (MUST be secure!)
JWT_SECRET=your-super-secret-jwt-key-CHANGE-THIS-IN-PRODUCTION
JWT_EXPIRES_IN=7d

# Agent communication secret - used for game server agent connections
AGENT_SECRET=your-agent-secret-key-CHANGE-THIS-IN-PRODUCTION
AGENT_PORT=8080

# Password hashing rounds - higher = more secure but slower (10-12 recommended)
BCRYPT_ROUNDS=12

# =============================================================================
# 🗄️  DATABASE CONFIGURATION (Multiple Database Support)
# =============================================================================

# Database Type Selection
# Supported: postgresql, mysql, mongodb, sqlite
DB_TYPE=postgresql

# Local vs Remote Database
# true = Use Docker containers (managed automatically)
# false = Use remote/existing database (provide connection details)
DB_LOCAL=true

# Database Connection Details
# For local Docker databases, these create the container configuration
# For remote databases, these are your existing database credentials
DB_HOST=postgres
DB_PORT=5432
DB_NAME=ctrl_alt_play
DB_USER=ctrlaltplay
DB_PASSWORD=ctrlaltplay

# Database-Specific Options
DB_VARIANT=postgres  # For MySQL: mysql or mariadb
DB_SSL=false        # Enable SSL for remote connections

# Direct Connection URL (Optional)
# If provided, overrides individual DB_* settings above
# Examples:
# DATABASE_URL="postgresql://user:pass@host:5432/dbname"
# DATABASE_URL="mysql://user:pass@host:3306/dbname"
# DATABASE_URL="mongodb://user:pass@host:27017/dbname"
# DATABASE_URL="file:./data/sqlite.db"
DATABASE_URL="postgresql://ctrlaltplay:ctrlaltplay@localhost:5432/ctrlaltplay"

# =============================================================================
# 🔴 REDIS CACHE CONFIGURATION
# =============================================================================

# Redis cache connection
REDIS_URL="redis://localhost:6379"

# =============================================================================
# 🛡️  SECURITY & PERFORMANCE (Good defaults provided)
# =============================================================================

# API rate limiting - requests per window per IP
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# File upload limits
MAX_FILE_SIZE=100MB
UPLOAD_PATH=./uploads

# Logging configuration
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# =============================================================================
# 📧 EMAIL SETTINGS (Optional - can configure later)
# =============================================================================
# 
# 💡 Leave blank to disable email notifications
# 🔧 Configure these for password resets and notifications
#

# SMTP server settings (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password
